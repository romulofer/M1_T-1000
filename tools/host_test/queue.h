/* See COPYING.txt for license details. */

/*
 * queue.h  (HOST TEST SHIM — not the FreeRTOS queue API)
 *
 * Satisfies the "queue.h" include in m1_infrared.h. Only the handle types are
 * needed; see app_freertos.h.
 *
 * NEVER part of the firmware build.
 *
 * M1 Project — host test harness
 */

#ifndef QUEUE_H_
#define QUEUE_H_

#include "app_freertos.h"

#endif /* QUEUE_H_ */
